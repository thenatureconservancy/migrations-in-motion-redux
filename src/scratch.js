{"VELOCITY_SCALE":0.03,"INTENSITY_SCALE_STEP":10,"MAX_WIND_INTENSITY":40,"MAX_PARTICLE_AGE":25,"PARTICLE_LINE_WIDTH":2.5,"PARTICLE_MULTIPLIER":0.3,"FRAME_RATE":60,"NULL_WIND_VECTOR":["__vue_devtool_nan__","__vue_devtool_nan__",null],"FADE_TRANSPARENCY":0.7,"COLOR_RAMP":"#47d4fc","COLOR_ALPHA":0.7,"spData":"data/perlin_4_west.json","spName":"birds"}

      // document.getElementById('amphibs').remove();
      // document.getElementById('mammals').remove();
      // document.getElementById('birds').remove();

      // this.amphibsActive = this.config.scene1.amphibs;
      // this.birdsActive = this.config.scene1.birds;
      // this.mammalsActive = this.config.scene1.mammals;

      // var amphibs = this.amphibsActiveWindy;
      // var birds = this.birdsActiveWindy;
      // var mammals = this.mammalsActiveWindy;

      // // // Build flow
      // this.buildFlow(self.map, 'birds', birds, true);
      // this.buildFlow(self.map, 'mammals', mammals, true);
      // this.buildFlow(self.map, 'amphibs', amphibs, true);

      // this.fadeOpacity('mapSatelliteOpacity', 'in', 10000);

      // setTimeout(function() {
      //   self.fadeOpacity('mapSatelliteOpacity', 'out', 15000);
      // }, 15000);

      // Test fade opacity on satellite layer
      // var opacityTimer = 0;
      // function updateOpacity() {
      //   opacityTimer ++;
      //   self.mapSatelliteOpacity =
      // }

      // setInterval(updateOpacity, 10);